name: Build Android App

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # ✅ الخطوة السحرية: إصلاح ملفات البناء تلقائياً
      # هذا الأمر يعيد بناء ملفات Gradle الناقصة ويربطها بـ Flutter بشكل صحيح
      - name: Repair Flutter Project
        run: |
          flutter pub get
          flutter create . --platforms=android --org=com.example

      # 1. فك تشفير google-services.json
      - name: Create google-services.json
        run: |
          echo "${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}" | base64 --decode > android/app/google-services.json

      # 2. فك تشفير Keystore
      - name: Decode Keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > android/upload-keystore.jks

      # 3. إنشاء ملف key.properties
      - name: Create key.properties
        run: |
          echo "storePassword=${{ secrets.STORE_PASSWORD }}" > android/key.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> android/key.properties
          echo "keyAlias=${{ secrets.KEY_ALIAS }}" >> android/key.properties
          echo "storeFile=../upload-keystore.jks" >> android/key.properties

      # 4. استعادة كود منع التصوير (لأن flutter create قد يمسحه)
      - name: Restore Secure Flag Code
        run: |
          # نستخدم أمر بسيط لإعادة كتابة ملف MainActivity وتضمين كود الحماية
          echo 'package com.example.edu_vantage_app

          import android.os.Bundle
          import android.view.WindowManager
          import io.flutter.embedding.android.FlutterActivity

          class MainActivity: FlutterActivity() {
              override fun onCreate(savedInstanceState: Bundle?) {
                  super.onCreate(savedInstanceState)
                  window.setFlags(WindowManager.LayoutParams.FLAG_SECURE, WindowManager.LayoutParams.FLAG_SECURE)
              }
          }' > android/app/src/main/kotlin/com/example/edu_vantage_app/MainActivity.kt

      # 5. بناء التطبيق
      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
