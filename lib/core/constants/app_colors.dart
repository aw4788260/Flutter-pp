import 'package:flutter/material.dart';

class AppColors {
  // الخلفيات (Dark Slate) المستخرجة من
  static const Color backgroundPrimary = Color(0xFF1F2933);   // bg-background-primary
  static const Color backgroundSecondary = Color(0xFF242F3D); // bg-background-secondary

  // الألوان المميزة (Accents)
  static const Color accentYellow = Color(0xFFE1AD01); // text-accent-yellow
  static const Color accentOrange = Color(0xFFD35400); // text-accent-orange

  // النصوص
  static const Color textPrimary = Color(0xFFE2E8F0);   // text-primary (أبيض مائل للرمادي)
  static const Color textSecondary = Color(0xFF94A3B8); // text-secondary (رمادي فاتح)

  // حالات النظام
  static const Color success = Color(0xFF22C55E); // Green for success
  static const Color error = Color(0xFFEF4444);   // Red for errors
}
